package com.semanticsquare.thrillio.partner;

public interface Shareable {
	String getItemData();
//	optional capability exclusive only to Book and Weblink instances so that content can be shared with partner websitesÏ
}
